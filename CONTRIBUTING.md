# 贡献指南

感谢参与改进小企业会计工具。提交代码前，请先确认贡献内容适合本项目的非商业、
源码互惠定位，并且不包含真实账套、票据、账号凭据或无权公开的材料。

## 开发流程

1. 从最新 `main` 分支创建功能分支。
2. 保持修改范围清晰，不要同时混入无关格式化或生成文件。
3. 新增或修复功能时补充相应自动化测试。
4. 提交前运行：

```powershell
python -m unittest discover -s tests -v
powershell -ExecutionPolicy Bypass -File .\scripts\check_public_release.ps1
```

5. 在拉取请求中说明用户问题、实现方式、验证结果和兼容性影响。

## 语义词库与会计口径

- 会计科目主档与语义词库必须保持分离；词库只负责自然语言到科目的映射。
- 新增或调整语义映射时，必须在
  `tests/fixtures/semantic_regression_small_enterprise.json` 增加固定回归语料。
- 明确规则必须给出科目和规则依据；模糊语义交给模型时，候选范围仍须来自当前
  《小企业会计准则》科目主档。
- 涉及税率、阈值、资格条件或政策日期的修改，必须附官方来源、适用期间，并保持
  参数可由用户修改，不得在界面中作合规或申报成功承诺。

## 数据与隐私

不得提交以下内容：

- 真实企业账套、银行流水、发票、纳税申报资料和备份；
- 用户名、密码摘要、Token、密钥、手机号、身份证号、真实税号或本机绝对路径；
- `models/`、`runtime/`、`data/`、`tmp/`、`build/`、`dist/`、`release/` 中的文件；
- 未确认再分发权的模型权重、OCR 权重、二进制或数据集。

测试票据必须是明确标注的合成数据，名称、税号、金额和票号不得对应真实主体。

## 贡献许可与签署

向本项目提交贡献，即表示您有权提交该内容，并同意该贡献按项目当前的
《小企业会计非商业互惠许可协议 1.0》向公众提供。您保留贡献的著作权。

如果您控制的专利申请或专利因使用您的贡献而不可避免地被实施，您同时按
`LICENSE` 第八条的相同非商业范围授予有限专利许可；该许可不扩展到商业使用或
本项目之外的独立实现。

提交信息应包含 Developer Certificate of Origin 风格的签署行：

```text
Signed-off-by: Your Name <your-email@example.com>
```

使用 `git commit -s` 可以自动添加该行。不要使用无权代表的组织名义签署。

