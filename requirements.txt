# 核心依赖
numpy>=1.21.0
pandas>=1.3.0
requests>=2.31.0

# GUI
# tkinter 随 Python Windows 安装提供，不是 pip 包。

# Excel导出
openpyxl>=3.0.0
xlrd>=2.0.1

# OCR 使用 runtime/ocr/python_deps 内的 Python 3.12 离线运行时依赖。
# 图片与扫描 PDF 由 RapidOCR + ONNX Runtime + PDFium 处理。

# 模型相关
# 无需额外安装，使用llama-server本地推理
