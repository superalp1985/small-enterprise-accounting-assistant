# 安全政策

## 支持范围

安全修复以最新公开版本为主。旧版本可能不会单独回补，请先确认问题能否在最新版本
复现。财税计算结果、政策适用争议和功能建议不属于安全漏洞，应使用普通议题模板。

## 私密报告漏洞

请优先使用 GitHub 仓库的 **Security -> Report a vulnerability** 私密漏洞报告功能。
仓库维护者在首次公开前应启用 Private vulnerability reporting。

不要在公开 Issue、截图、日志或附件中提交：

- 真实账套、发票、银行流水、税号、手机号或身份证号；
- `security/credentials.json`、密码、摘要、Token、密钥或本机用户名路径；
- 可直接利用、且尚未修复的完整攻击脚本。

报告中请提供受影响版本、Windows 版本、最小复现步骤、预期影响和已经采取的临时缓解
措施。请使用合成账套和脱敏日志复现。

## 安全边界

本机账号登录用于限制软件入口，密码以带盐 PBKDF2 摘要保存，但它不等同于磁盘加密、
操作系统账户隔离或数据库透明加密。能够读取当前 Windows 用户目录的攻击者仍可能读取
账套文件。高敏感数据应配合 BitLocker、Windows 账户权限和离线备份使用。

